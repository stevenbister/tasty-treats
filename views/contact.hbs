<h1>{{title}}</h1>

<form action="/contact" method="post">
  <label for="">Name</label>
  <input type="text" name="name">
  {{#if errors.name }}
  {{ errors.name.msg }}
  {{/if}}
  <button>submit</button>
</form>